# Revora Backend Environment Variables
PORT=8000
CORS_ORIGINS=*
# Optional: Path to SQLite database if mounted on persistent disk (e.g. /var/data/revora.db)
# REVORA_DB_PATH=/var/data/revora.db

# AI & Gateway Credentials (Optional / Sandbox)
GEMINI_API_KEY=
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
